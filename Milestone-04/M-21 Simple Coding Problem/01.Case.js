const details = 'I live in Bangladesh.It is a independent country';

const country = 'Bangladesh';
const country1 = 'BanGladeSh';
console.log(country.toLocaleUpperCase());
if (country.toLocaleLowerCase() === country1.toLocaleLowerCase()) {
    console.log('Valid Input User');
} else {
    console.log('Invalid User Input');
}